package controllers;

public class deleteMe {
     /*   private Resident getResidentByName(String name) {
        Resident resident = residentRepo.findById(name);
        if (resident == null) throw new ResidentDoesNotExistException("Resident not found");
        if (residentRepo.findByName(resident.getPhoneNumber()) != null)
            throw new ResidentAlreadyRegisteredException("Resident already registered");
        return resident;
    }

    private Resident getResidentByPhoneNumber(String PhoneNumber) {
        Resident resident = residentRepo.findById(PhoneNumber);
        if (resident == null) throw new ResidentDoesNotExistException("Resident not found");
        if (residentRepo.findByPhone(resident.getPhoneNumber()) != null)
            throw new ResidentAlreadyRegisteredException("Resident already registered");
        return resident;
    }

    private Resident getResidentByEmail(String PhoneNumber) {
        Resident resident = residentRepo.findById(PhoneNumber);
        if (resident == null) throw new ResidentDoesNotExistException("Resident not found");
        if (residentRepo.findByEmail(resident.getEmail()) != null)
            throw new ResidentAlreadyRegisteredException("Resident already registered");
        return resident;
    }*/
}
